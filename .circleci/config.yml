version: 2.1

jobs:

  build:

    docker:
      - image: circleci/python:3.7.4

    steps:
      - checkout

      - run: sudo pip install pipenv

      - run: make install

      - run: make check-style

      - run: make test
